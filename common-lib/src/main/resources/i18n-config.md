spring:
  messages:
    basename: messages/messages
    encoding: UTF-8
    fallback-to-system-locale: false
  web:
    locale: en
    locale-resolver: fixed

---

# English
messages_en.properties = app.name=FoodBox

# Hindi
messages_hi.properties = app.name=फूडबॉक्स

# Spanish
messages_es.properties = app.name=FoodBox
